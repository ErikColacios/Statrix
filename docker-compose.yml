services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: Dockerfile.web
    expose:
      - "3000"

  chat:
    container_name: chat
    build:
      context: .
      dockerfile: Dockerfile.chat
    expose:
      - "4000"

  nginx:
    image: nginx:alpine
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
      - chat

# volumes:
#   db-data:
#     driver: flocker
#     driver_opts:
#       size: "10GiB"

# configs:
#   httpd-config:
#     external: true

# secrets:
#   server-certificate:
#     external: true